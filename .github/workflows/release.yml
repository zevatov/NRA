name: Release Builder

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    name: Build Release Binaries
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - os: ubuntu-latest
            os_name: linux
            ext: ""
          - os: macos-latest
            os_name: macos
            ext: ""
          - os: windows-latest
            os_name: windows
            ext: ".exe"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux Dependencies
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config libglib2.0-dev libgtk-3-dev

      - name: Build NRA CLI
        run: cargo build --release -p nra-cli

      - name: Build NRA Manager Pro
        run: cargo build --release -p nra-gui

      - name: Rename and Prepare Assets
        shell: bash
        run: |
          mkdir -p release_assets
          cp target/release/nra-cli${{ matrix.ext }} release_assets/nra-cli-${{ matrix.os_name }}${{ matrix.ext }}
          cp target/release/nra-gui${{ matrix.ext }} release_assets/nra-manager-pro-${{ matrix.os_name }}${{ matrix.ext }}

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v2
        with:
          files: release_assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
